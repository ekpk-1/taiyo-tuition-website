name: Deploy to GitHub Pages

on:
    push:
        branches: [website-rbuild] # Changed to match your current branch
    pull_request:
        branches: [website-rbuild]

# Allow this job to clone the repo and create a page deployment
permissions:
    contents: write
    pages: write
    id-token: write
    actions: read

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
concurrency:
    group: "pages"
    cancel-in-progress: false

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "18"
                  cache: "npm"

            - name: Install dependencies
              run: npm ci

            - name: Build
              run: npm run build
              env:
                  GITHUB_PAGES: true
                  VITE_EMAILJS_SERVICE_ID: ${{ secrets.VITE_EMAILJS_SERVICE_ID }}
                  VITE_EMAILJS_PUBLIC_KEY: ${{ secrets.VITE_EMAILJS_PUBLIC_KEY }}
                  VITE_EMAILJS_TEMPLATE_ID_ADMIN: ${{ secrets.VITE_EMAILJS_TEMPLATE_ID_ADMIN }}
                  VITE_EMAILJS_TEMPLATE_ID_USER: ${{ secrets.VITE_EMAILJS_TEMPLATE_ID_USER }}

            # SPA route entrypoints (/contact, /enroll, etc.) are created by
            # npm postbuild so Google Ads destination checks get HTTP 200.

            - name: Deploy to gh-pages
              uses: peaceiris/actions-gh-pages@v3
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  publish_dir: ./dist
                  publish_branch: gh-pages
                  force_orphan: true
